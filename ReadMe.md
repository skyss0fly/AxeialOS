# Note: this Branch is an Unofficial Rewrite of AxeialOS Rewritten for RustLang. I've also cleaned up the branch to automate the building and testing process.
## README INCOMPLETE
# Bootloader:
This Fork uses the Limine Bootloader

# Required Dependencies:


# Dependency Install:
run `dependencies.sh` to auto install them to your device 

# Building:
run `build.sh` to Manually build and make it into a bootable ISO Image

# Running:
either run `buildandboot.sh` to build and boot or run `run.sh` to run based off latest ISO Image built
